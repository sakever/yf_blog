// 侧边栏字体调小（主题默认偏大，在此覆盖）
// 如需进一步调整，直接修改下面数值即可（数值越小字越小）
.sidebar
  // 一级分组标题（「网络」「Java」等），主题默认 1.1em
  .sidebar-heading
    font-size 0.95em !important
  // 文章标题链接，主题默认 1em
  a.sidebar-link
    font-size 0.9em !important
  // 文章内 h2/h3 子标题，主题默认 0.95em
  .sidebar-sub-headers
    font-size 0.85em !important
